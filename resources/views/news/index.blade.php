@extends('layouts.app')
@section('title', 'Новости — Комплекс Греция')
@section('content')
<section class="page-hero"><div class="container"><div class="eyebrow">Жизнь комплекса</div><h1>Новости и полезные материалы</h1><p>Расписание мероприятий, советы тренеров, новые процедуры и специальные предложения.</p></div></section><section class="section-padding"><div class="container"><div class="row g-4">@forelse ($posts as $post)<div class="col-md-6 col-xl-4"><article class="content-card h-100">@if ($post->image_path)<a href="{{ route('news.show', $post) }}"><img src="{{ Storage::url($post->image_path) }}" alt="{{ $post->title }}"></a>@endif<div class="content-card-body"><small>{{ optional($post->published_at)->translatedFormat('d F Y') }}</small><h2><a href="{{ route('news.show', $post) }}">{{ $post->title }}</a></h2><p>{{ $post->excerpt ?: Str::limit(strip_tags($post->body), 150) }}</p><a class="text-link" href="{{ route('news.show', $post) }}">Читать <i class="bi bi-arrow-right"></i></a></div></article></div>@empty<div class="col-12"><div class="empty-state"><i class="bi bi-newspaper"></i><h3>Новости скоро появятся</h3><p>Администратор может добавить первую публикацию в CRM.</p></div></div>@endforelse</div><div class="mt-5">{{ $posts->links() }}</div></div></section>
@endsection
