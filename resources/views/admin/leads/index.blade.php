@extends('admin.layout')
@section('title', 'Лиды') @section('heading', 'Входящие обращения') @section('eyebrow', 'Воронка продаж')
@section('content')
<div class="admin-card"><div class="admin-card-header flex-wrap gap-3"><div><h3>Лиды</h3><p>Заявки на обратный звонок и подбор времени</p></div><form><select class="form-select" name="status" onchange="this.form.submit()"><option value="">Все статусы</option>@foreach(['new'=>'Новые','contacted'=>'Связались','qualified'=>'Целевые','won'=>'Продажа','lost'=>'Отказ'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select></form></div><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>Контакт</th><th>Запрос</th><th>Источник</th><th>Создан</th><th>Работа с лидом</th></tr></thead><tbody>@forelse($leads as $lead)<tr><td><strong>{{ $lead->name }}</strong><small><a href="tel:{{ $lead->phone }}">{{ $lead->phone }}</a></small></td><td style="min-width:240px">{{ $lead->request ?: 'Перезвонить и подобрать время' }}</td><td>{{ $lead->channel }}</td><td>{{ $lead->created_at->format('d.m.Y H:i') }}</td><td><form method="post" action="{{ route('admin.leads.update',$lead) }}" class="d-flex gap-2">@csrf @method('patch')<select class="form-select form-select-sm" name="status">@foreach(['new'=>'Новый','contacted'=>'Связались','qualified'=>'Целевой','won'=>'Продажа','lost'=>'Отказ'] as $value=>$label)<option value="{{ $value }}" @selected($lead->status===$value)>{{ $label }}</option>@endforeach</select><input class="form-control form-control-sm" name="notes" value="{{ $lead->notes }}" placeholder="Комментарий"><button class="btn btn-sm btn-dark"><i class="bi bi-check2"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">Обращений нет</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $leads->links() }}</div></div>
@endsection
