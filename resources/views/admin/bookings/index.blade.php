@extends('admin.layout')
@section('title', 'Записи') @section('heading', 'Записи клиентов') @section('eyebrow', 'Расписание посещений')
@section('content')
<div class="admin-card"><div class="admin-card-header flex-wrap gap-3"><div><h3>Все записи</h3><p>Подтверждайте, отменяйте и отмечайте посещение</p></div><form class="d-flex gap-2"><input type="date" class="form-control" name="date" value="{{ request('date') }}"><select class="form-select" name="status"><option value="">Все статусы</option>@foreach(['new'=>'Новая','confirmed'=>'Подтверждена','completed'=>'Завершена','cancelled'=>'Отменена','no_show'=>'Не пришёл'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="btn btn-primary">Найти</button></form></div><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>Дата</th><th>Клиент</th><th>Услуга</th><th>Гости</th><th>Сумма</th><th>Управление</th></tr></thead><tbody>@forelse($bookings as $booking)<tr><td><strong>{{ $booking->slot->starts_at->format('d.m.Y') }}</strong><small>{{ $booking->slot->starts_at->format('H:i') }}–{{ $booking->slot->ends_at->format('H:i') }}</small></td><td><strong>{{ $booking->customer->name }}</strong><small><a href="tel:{{ $booking->customer->phone }}">{{ $booking->customer->phone }}</a></small></td><td>{{ $booking->service->name }}@if($booking->trainer)<small>{{ $booking->trainer->name }}</small>@endif</td><td>{{ $booking->people }}</td><td>{{ number_format($booking->total,0,',',' ') }} ₽</td><td><form method="post" action="{{ route('admin.bookings.update',$booking) }}" class="d-flex gap-2">@csrf @method('patch')<select class="form-select form-select-sm" name="status">@foreach(['new'=>'Новая','confirmed'=>'Подтверждена','completed'=>'Завершена','cancelled'=>'Отменена','no_show'=>'Не пришёл'] as $value=>$label)<option value="{{ $value }}" @selected($booking->status===$value)>{{ $label }}</option>@endforeach</select><select class="form-select form-select-sm" name="payment_status">@foreach(['unpaid'=>'Не оплачено','pending'=>'Ожидает','paid'=>'Оплачено','refunded'=>'Возврат'] as $value=>$label)<option value="{{ $value }}" @selected($booking->payment_status===$value)>{{ $label }}</option>@endforeach</select><button class="btn btn-sm btn-dark"><i class="bi bi-check2"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Записи не найдены</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $bookings->links() }}</div></div>
@endsection
