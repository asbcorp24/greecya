@extends('admin.layout')
@section('title', 'Расписание') @section('heading', 'Расписание') @section('eyebrow', 'Свободные слоты')
@section('content')
<div class="row g-4"><div class="col-xl-4"><div class="admin-card p-4 sticky-xl-top" style="top:1rem"><h3>Добавить время</h3><p class="text-muted">Слот сразу появится на странице онлайн-записи.</p><form method="post" action="{{ route('admin.schedule.store') }}" class="row g-3">@csrf<div class="col-12"><label class="form-label">Услуга</label><select class="form-select" name="service_id" required>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Тренер</label><select class="form-select" name="trainer_id"><option value="">Без тренера</option>@foreach($trainers as $trainer)<option value="{{ $trainer->id }}">{{ $trainer->name }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Начало</label><input type="datetime-local" class="form-control" name="starts_at" required></div><div class="col-12"><label class="form-label">Окончание</label><input type="datetime-local" class="form-control" name="ends_at" required></div><div class="col-12"><label class="form-label">Вместимость</label><input type="number" class="form-control" name="capacity" min="1" max="100" value="1" required></div><div class="col-12"><button class="btn btn-primary w-100">Добавить в расписание</button></div></form></div></div><div class="col-xl-8"><div class="admin-card"><div class="admin-card-header"><div><h3>Предстоящие слоты</h3><p>Занято / всего мест</p></div></div><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>Дата и время</th><th>Услуга</th><th>Тренер</th><th>Места</th><th></th></tr></thead><tbody>@forelse($slots as $slot)<tr><td><strong>{{ $slot->starts_at->format('d.m.Y H:i') }}</strong><small>до {{ $slot->ends_at->format('H:i') }}</small></td><td>{{ $slot->service->name }}</td><td>{{ $slot->trainer?->name ?? '—' }}</td><td><span class="count-badge">{{ $slot->booked_count }} / {{ $slot->capacity }}</span></td><td>@if($slot->booked_count===0)<form method="post" action="{{ route('admin.schedule.destroy',$slot) }}" onsubmit="return confirm('Удалить время?')">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">Расписание пусто</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $slots->links() }}</div></div></div></div>
@endsection
