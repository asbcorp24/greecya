@extends('admin.layout')
@section('title','Семьи') @section('heading','Семейные аккаунты') @section('eyebrow','Родители, дети и общий кошелёк')
@section('content')
<div class="row g-4"><div class="col-xl-4"><div class="admin-card p-4"><h3>Новая семья</h3><form method="post" action="{{ route('admin.families.store') }}" class="row g-2">@csrf<div class="col-12"><input class="form-control" name="name" placeholder="Семья Ивановых" required></div><div class="col-12"><select class="form-select" name="primary_customer_id" required><option value="">Основной родитель</option>@foreach($customers as $c)<option value="{{ $c->id }}">{{ $c->name }} · {{ $c->phone }}</option>@endforeach</select></div><div class="col-12"><textarea class="form-control" name="notes" placeholder="Комментарий"></textarea></div><div class="col-12"><button class="btn btn-primary w-100">Создать семейный аккаунт</button></div></form></div></div>
<div class="col-xl-8"><div class="admin-card p-4"><div class="d-flex justify-content-between gap-3 flex-wrap mb-3"><div><h3>Семьи</h3><p class="text-muted">Общие абонементы, дети, согласия и кошелёк</p></div><form class="d-flex gap-2"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Семья, ФИО, телефон"><button class="btn btn-outline-primary">Найти</button></form></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Семья</th><th>Основной клиент</th><th>Участники</th><th>Кошелёк</th><th></th></tr></thead><tbody>@forelse($families as $family)<tr><td><strong>{{ $family->name }}</strong><small class="d-block">{{ $family->status }}</small></td><td>{{ $family->primaryCustomer?->name }}</td><td>@foreach($family->members as $member)<span class="badge text-bg-light border">{{ $member->customer?->name }} · {{ $member->relation }}</span> @endforeach</td><td>{{ number_format((float)optional($family->wallet)->deposit_balance,0,',',' ') }} ₽<small class="d-block">{{ number_format((float)optional($family->wallet)->bonus_balance,0,',',' ') }} бонусов</small></td><td><a class="btn btn-sm btn-primary" href="{{ route('admin.families.show',$family) }}">Открыть</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Семей пока нет.</td></tr>@endforelse</tbody></table></div>{{ $families->links() }}</div></div></div>
@endsection
